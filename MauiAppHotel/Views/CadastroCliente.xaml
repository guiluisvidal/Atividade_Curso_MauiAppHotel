<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MauiAppHotel.Views.CadastroCliente"
             Title="CadastroCliente"
             NavigationPage.HasNavigationBar="False"
             BackgroundColor="{StaticResource MidnightBlue}">

    <VerticalStackLayout Padding="20" Spacing="10">
        <Label Text="Cadastro do Cliente"
               FontSize="32"
               TextColor="{StaticResource White}"
               HorizontalOptions="Center" />

        <Entry x:Name="entryNome" Placeholder="Nome" />
        <Entry x:Name="entryIdade" Placeholder="Idade" Keyboard="Numeric" />
        <Entry x:Name="entryTelefone" Placeholder="Telefone" Keyboard="Telephone" />
        <Entry x:Name="entryRg" Placeholder="RG" />

        <Button Text="Avançar" Clicked="OnAvancarClicked" />
    </VerticalStackLayout>
</ContentPage>
